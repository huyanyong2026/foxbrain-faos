# VAFOX Huyan Portal V2.0.2 — CEO AI 经营日报增强方案

> 文档版本：V2.0.2  
> 交付类型：设计，不包含生产系统改造  
> 目标用户：CEO / 授权管理层  
> 生成时间：每日 07:00（`Asia/Shanghai`）  
> 前置能力：V2.0.1 AI 顾问  
> 设计状态：`DESIGN_READY_FOR_REVIEW`

## 1. 设计结论

在 Huyan Portal 新增独立的 **CEO Daily Report（CEO AI 经营日报）** 能力，以只读方式消费现有数据与 AI Runtime 能力，每日 07:00 生成一份可追溯、可降级、不可伪造的管理层日报。日报固定包含“经营总结、经营机会、经营风险、CEO 行动建议”四部分；每部分统一使用“结论 / 依据 / 建议”结构，并在报告级和条目级明确展示 `data_source`、`updated_at`、`freshness_status`、`confidence`。

本方案不修改 CEO Today 核心页面，不建立新的事实数据源，不直连 SAP B1、SAP Mirror 或 Data Core，不改变 Auth 与 AI Runtime 基础配置。V2.0.1 AI 顾问继续保持原入口和能力；日报是新的独立只读页面与编排流程，不替换 AI 顾问，也不把对话回答直接发布为日报。

## 2. 目标、原则与范围

### 2.1 目标

1. 让 CEO 在一个页面内看到跨销售、商品、库存、客户、员工、供应链六个经营域的当日管理摘要。
2. 将事实、AI 判断与建议严格分层，使每项判断都能回到具体依据与数据血缘。
3. 在数据延迟、缺失、冲突或生成失败时显式降级，绝不补造数字或结论。
4. 通过 07:00 定时生成、幂等发布、审计留痕和权限控制，形成可运营的生产方案。

### 2.2 设计原则

- **事实唯一**：事实只来自现有获准的只读服务合同；日报不维护第二套经营事实。
- **证据先行**：没有有效依据，不输出确定性经营结论；没有建议依据，不输出行动建议。
- **血缘可见**：报告与每个条目都必须携带四个溯源字段。
- **鲜度约束结论**：鲜度影响可生成性、措辞强度和 `confidence` 上限。
- **只读与最小权限**：仅 CEO 或明确获得日报查看权限的管理层可访问。
- **失败关闭**：权限不明、合同不完整或数据不可验证时拒绝发布，而非降级到静态内容。
- **人机职责清晰**：AI 负责归纳、排序与建议；事实计算、权限判断、鲜度判断和发布门禁由确定性逻辑负责。

### 2.3 本期范围

- 独立 CEO Daily Report 页面及历史日期切换设计。
- 六类数据的只读聚合、证据标准化和跨域分析编排。
- 四个固定日报板块及统一内容格式。
- 每日 07:00 生成、重试、幂等、发布与审计策略。
- CEO / 授权管理层访问控制。
- API、数据合同、状态机、异常态和验收标准。

### 2.4 硬边界与禁止项

以下能力 **禁止修改**：

- SAP B1；
- SAP Mirror；
- Data Core；
- Auth；
- AI Runtime 基础配置；
- CEO Today 核心页面。

以下实现 **禁止出现**：

- mock、fixture、faker；
- 静态经营数字或失败时的示例数字回填；
- 浏览器直连数据库或源系统；
- AI 自行补齐缺失事实、推算未获准指标或改写源数据；
- 因日报生成而写回 SAP、自动下单、自动调价、自动触达客户或自动执行人事动作；
- 绕过现有 Gateway、Auth、数据域权限或 AI Runtime 审计链。

## 3. 产品与信息架构

### 3.1 独立入口

新增一级入口 **CEO Daily Report**，推荐路由为 `/daily-report`。入口与页面均为新增能力，不改变 `/` 中 CEO Today 的组件、数据请求、排序、指标、交互或样式。若导航框架需要增加入口，只允许修改共享导航清单；不得在 CEO Today 业务组件中嵌入或替换日报内容。

V2.0.1 AI 顾问保持独立入口。日报条目可提供“向 AI 顾问追问”跳转，并仅传递报告 ID、条目 ID 和用户可见的上下文引用；AI 顾问必须重新执行现有权限校验，不能因跳转获得额外数据范围。

### 3.2 页面层级

1. **页头**：报告日期、生成状态、生成完成时间、整体鲜度、整体置信度。
2. **数据状态条**：六个数据域的 `data_source`、`updated_at`、`freshness_status` 与可用状态。
3. **经营总结**：跨域经营态势的最高层归纳。
4. **经营机会**：按预期价值、时效性、证据质量和可执行性排序。
5. **经营风险**：按影响、紧迫性、发生可能性和证据质量排序。
6. **CEO 行动建议**：只呈现需要 CEO 决策、授权、协调或关注的动作。
7. **报告说明**：口径、覆盖范围、缺失域、生成批次和审计编号。

页面默认展示最近一份 `published` 报告；选择历史日期时读取该日已发布版本，不以当前数据重新覆盖历史事实。无已发布报告时展示明确空状态和原因，不展示模板化经营文案。

### 3.3 统一内容卡

四个板块均使用同一语义合同：

| 字段 | 展示名称 | 规则 |
| --- | --- | --- |
| `conclusion` | 结论 | 一句话说明发现；必须可由 `evidence` 支撑 |
| `evidence[]` | 依据 | 展示事实指标、比较口径、所属域与证据引用；禁止只写“AI 分析认为” |
| `recommendation` | 建议 | 与结论直接对应；无安全建议时明确为“暂不建议行动，等待数据恢复” |
| `data_source[]` | 数据来源 | 列出实际参与该条目的来源，不使用笼统的“系统数据” |
| `updated_at` | 更新时间 | 取该条目所用证据中最早的有效更新时间，避免用新时间掩盖旧证据 |
| `freshness_status` | 数据鲜度 | `fresh`、`delayed`、`stale`、`missing` 或 `conflict` |
| `confidence` | 可信度 | `high`、`medium`、`low` 或 `unavailable`，并展示理由 |

经营机会和经营风险允许有多张卡；经营总结至少一张综合卡；CEO 行动建议按优先级排列。所有卡片必须保持相同的“结论 / 依据 / 建议”阅读顺序，溯源字段置于卡片页脚并可展开查看证据详情。

## 4. 六类数据来源与只读映射

日报只通过 Gateway 调用现有、经授权的业务 API。下表是逻辑映射而非新建数据库或要求修改上游；实施阶段必须以已部署合同注册表为准，不存在的字段视为缺失，禁止在日报层猜测。

| 数据域 | 只读能力 | 可形成的依据 | 不允许推断 |
| --- | --- | --- | --- |
| 销售 | 现有销售分析合同 | 销售、订单、客单、门店/渠道/时段变化及合同返回的比较口径 | 未返回的利润、目标达成或增长原因 |
| 商品 | 现有商品分析合同 | 品牌、品类、SKU 表现及商品结构 | 未返回的成本、毛利或生命周期 |
| 库存 | 现有库存分析合同 | 库存金额、有效库存、滞销、缺货、周转及合同状态 | 用商品数量代替库存金额，或臆测可售库存 |
| 客户 | 现有客户分析/客户智能合同 | 客户分层、复购、流失信号、机会及授权范围内的 Customer 360 摘要 | 客户敏感信息、未授权画像或虚构购买意愿 |
| 员工 | 现有组织分析合同 | 员工销售、团队表现、组织覆盖与合同返回的异常 | 绩效定性、奖惩、离职概率或受保护的人事属性 |
| 供应链 | 现有供应链智能合同 | 供应商、采购、到货、库存协同与合同返回的风险 | 未返回的交期承诺、采购成本或供应商责任归因 |

每个上游响应进入日报前必须通过四项门禁：合同校验、用户数据范围校验、鲜度校验、证据完整性校验。浏览器只请求日报 API，不并发拼装六域事实；服务端编排层负责在同一审计上下文内获取数据。

## 5. 日报内容设计

### 5.1 经营总结

**结论**：对六域有效证据进行跨域归纳，说明当前经营态势和最值得 CEO 注意的变化。若有效域不足以支持全局判断，结论必须限定范围，例如明确“仅基于已就绪数据域”，不得表述为全公司结论。

**依据**：优先列出能够直接支持结论的跨域证据；每条依据包含指标标识、值或状态、比较基线、时间窗口、维度、来源引用。比较基线必须由上游合同提供或由确定性聚合按批准口径计算。

**建议**：给出当日关注顺序或需要进一步核查的方向；不得把缺失数据包装成经营改善建议。

### 5.2 经营机会

**结论**：识别合同事实支持的增长、转化、库存协同、客户经营、人员协同或供应链优化机会，并明确机会适用范围。

**依据**：至少包含一个直接证据；跨域机会必须分别引用相关数据域。排序分数由配置化的价值、时效、可执行性和证据质量因子计算，页面不把内部排序分数冒充财务收益。

**建议**：建议一个可验证的下一步动作，说明责任角色、建议完成窗口和验证指标；责任人只能引用组织与权限系统提供的角色信息，不自动指派具体员工。

### 5.3 经营风险

**结论**：描述已观察到的异常或风险信号、影响对象和紧迫性，区分“事实异常”与“预测风险”。来源冲突本身应作为数据风险展示，不能强行选择一个来源。

**依据**：引用异常值、趋势、时间窗口、影响范围及来源；预测风险额外展示模型/规则标识与不确定性。无法取得可靠依据时不生成业务风险卡，仅生成数据质量状态。

**建议**：优先给出核查、止损评估或管理协调建议。任何涉及交易、采购、定价、客户触达、人事处置的动作都只能作为待审批建议，不能自动执行。

### 5.4 CEO 行动建议

**结论**：将机会与风险中确需 CEO 决策、授权或跨部门协调的事项提升为行动建议；普通运营动作留在对应板块，不占用 CEO 行动队列。

**依据**：必须引用来源条目 ID，并继承其全部证据与溯源字段；不得生成与前三部分无关联的“通用管理建议”。

**建议**：使用“决策动作 + 责任角色 + 建议时限 + 验证方式”表达。仅提供“查看详情、向 AI 顾问追问、复制建议”类只读操作；未来若增加任务创建，需另行安全评审和人工确认，不在 V2.0.2 范围内。

## 6. 数据合同

### 6.1 报告响应

推荐新增只读接口：

```http
GET /api/ceo/daily-report/latest
GET /api/ceo/daily-report?report_date=YYYY-MM-DD
```

响应合同：

```json
{
  "report_id": "opaque-id",
  "report_date": "YYYY-MM-DD",
  "timezone": "Asia/Shanghai",
  "status": "published",
  "generated_at": "RFC3339 timestamp",
  "data_source": ["actual source identifiers"],
  "updated_at": "RFC3339 timestamp",
  "freshness_status": "fresh|delayed|stale|missing|conflict",
  "confidence": {
    "level": "high|medium|low|unavailable",
    "reason": "deterministic explanation"
  },
  "source_status": [],
  "sections": {
    "business_summary": [],
    "business_opportunities": [],
    "business_risks": [],
    "ceo_actions": []
  },
  "coverage": {
    "included_domains": [],
    "excluded_domains": [],
    "reason": "coverage explanation"
  },
  "audit_id": "opaque-id"
}
```

JSON 中的字符串仅表示类型与枚举，不是可投产的静态经营内容。生产响应必须由真实源合同及本次生成批次产生。

### 6.2 条目合同

每个 section item 至少包含：

```json
{
  "item_id": "opaque-id",
  "priority": "critical|high|normal",
  "conclusion": "generated from evidence",
  "evidence": [
    {
      "evidence_id": "opaque-id",
      "domain": "sales|product|inventory|customer|employee|supply_chain",
      "metric_key": "contract metric identifier",
      "observed_value": "typed value from source",
      "comparison": "typed comparison supplied or deterministically derived",
      "scope": "authorized scope",
      "source_ref": "non-sensitive trace reference",
      "updated_at": "RFC3339 timestamp"
    }
  ],
  "recommendation": "evidence-bound recommendation",
  "data_source": ["actual source identifiers"],
  "updated_at": "RFC3339 timestamp",
  "freshness_status": "fresh|delayed|stale|missing|conflict",
  "confidence": {
    "level": "high|medium|low|unavailable",
    "reason": "deterministic explanation"
  },
  "related_item_ids": []
}
```

所有自由文本必须经过证据引用校验。`evidence` 为空、来源不存在、时间不可解析或越权的条目不得进入 `published` 报告。

### 6.3 四个必备溯源字段的聚合规则

- `data_source`：条目取证据来源去重集合；报告取已发布条目来源与数据状态来源的去重集合。
- `updated_at`：条目取所用证据最早更新时间；报告取所有已纳入数据域的最早更新时间。
- `freshness_status`：采用最保守状态聚合；优先级为 `conflict` → `missing` → `stale` → `delayed` → `fresh`。
- `confidence`：由确定性规则根据证据完整度、鲜度、来源一致性、覆盖域和分析校验结果计算等级及理由；AI 不得自行提高等级。

## 7. 鲜度与可信度策略

### 7.1 鲜度

每个数据域的鲜度阈值必须引用现有数据合同或运维配置，不在前端、Prompt 或本文中硬编码业务时限。编排器用 `generated_at - source.updated_at` 与该域的获批 SLA 比较：

- `fresh`：满足该域 SLA；
- `delayed`：超过正常更新时间但仍处于获准分析窗口；
- `stale`：超过获准分析窗口，禁止形成强结论；
- `missing`：无数据、无 `updated_at` 或合同校验失败；
- `conflict`：同一事实的获准来源发生不可解释冲突。

若任一关键证据为 `stale`，对应卡片最高为 `low`；`missing` 或 `conflict` 的事实不得用于业务结论。报告可在部分域缺失时有限发布，但必须展示覆盖范围，且综合结论不得声称覆盖缺失域。关键域清单和最低覆盖要求应由产品、数据治理与 CEO 办公室审批后配置化管理。

### 7.2 可信度

`confidence` 不是模型主观自评，而是发布门禁的输出：

- `high`：证据完整、鲜度满足要求、来源一致、结论与引用校验通过；
- `medium`：证据有效但覆盖、比较口径或跨域一致性有限；
- `low`：仅能给出范围受限的观察或数据延迟提示；
- `unavailable`：无法形成可验证判断，不输出经营结论。

UI 同时展示等级与 `reason`，禁止只用颜色表达。整体可信度不得高于所依赖条目中的最低可信等级。

## 8. 07:00 生成与发布流程

### 8.1 调度

- 调度标识：`ceo_daily_report_0700`。
- Cron 语义：每日 07:00，时区固定为 `Asia/Shanghai`，不得依赖服务器本地时区。
- 报告业务日期由 07:00 触发时的上海日期确定。
- 唯一幂等键：`report_date + timezone + contract_version + authorized_scope_version`。
- 多实例使用现有基础设施支持的分布式互斥/唯一约束，确保同一幂等键只有一个发布版本。

本文只定义调度使用方式，不要求修改 AI Runtime 基础配置。具体调度器应复用获准的现有作业平台；若生产尚未批准自动调度，则先以同一编排入口进行授权人工触发，审批后再启用 07:00 作业。

### 8.2 流程

1. **创建批次**：记录业务日期、合同版本、授权服务身份和审计 ID。
2. **权限预检**：确认作业身份仅具有六域只读范围及日报写入自身存储的权限。
3. **读取数据**：经 Gateway 请求六域现有合同，保留源时间、来源和 trace reference。
4. **合同与鲜度校验**：拒绝非法类型、缺少血缘或超出范围的数据。
5. **证据标准化**：将有效事实转换为带类型、范围和引用的 evidence records。
6. **确定性候选生成**：通过已批准的指标、比较与异常规则产生候选；规则版本写入审计。
7. **AI 归纳**：通过现有 AI Runtime 对候选进行摘要、归类、去重和建议生成，不改变事实值。
8. **输出校验**：检查四板块、统一格式、引用闭合、敏感信息、禁止动作、四个溯源字段。
9. **发布门禁**：满足最低覆盖及证据标准则原子发布；否则记录 `degraded` 或 `failed`，不暴露半成品。
10. **审计与可观测**：记录耗时、来源状态、模型与 Prompt 版本标识、规则版本、token/调用统计、校验结果和错误码；日志不得包含不必要的客户或员工明细。

### 8.3 状态机与重试

状态为 `scheduled → collecting → validating → generating → quality_gate → published`；异常进入 `degraded` 或 `failed`。只有 `published` 可作为正常日报展示，`degraded` 仅在其内容通过有限发布门禁时展示并明确缺失范围。

对瞬时网络错误或上游暂不可用执行有上限的退避重试；重试次数与间隔使用运维配置，禁止在页面硬编码。重试始终复用同一幂等键。超过重试上限后保留上一份历史报告，但页面必须标明其原报告日期和鲜度，不能把昨日内容伪装成今日报告。

## 9. 权限、安全与隐私

### 9.1 访问控制

- 允许：CEO 角色，或由现有 Auth 明确授予 `ceo_daily_report:read` 的管理层角色。
- 拒绝：未登录、权限不明、授权已撤销、仅有普通分析权限的用户。
- 服务端在每次列表和详情请求中强制校验身份、角色与经营数据范围；前端隐藏入口不是授权手段。
- 建议返回：未认证 `401`，已认证但无权 `403`，请求范围越界同样 `403`。
- 历史报告访问遵循“查看时权限”，不能因用户生成时曾有权限而永久可见。

本方案复用现有 Auth，不新增身份库、不改变 token、不在日报服务复制角色关系。授权管理层的授予、撤销与审计继续由现有 Auth 治理流程完成。

### 9.2 数据最小化

- 默认使用聚合指标；客户与员工证据只展示决策所需的最少识别信息。
- 不把完整源响应发送给 AI Runtime，只发送已授权且标准化的证据集合。
- `source_ref` 使用不可猜测的内部引用，不暴露数据库键、连接信息或敏感路径。
- 下载、复制和向 AI 顾问追问均记录审计；V2.0.2 不提供批量原始数据导出。

### 9.3 Prompt 与输出安全

源字段按不可信数据处理，禁止将商品名、客户备注、员工文本等当作系统指令。Runtime 输入必须区分 instruction 与 evidence；输出执行结构化 Schema 校验、引用校验、权限校验、敏感信息过滤和禁止动作检查。任何校验失败都阻止对应条目发布。

## 10. 页面状态与交互

| 状态 | 页面行为 |
| --- | --- |
| 加载中 | 显示结构骨架，不显示上次数字作为占位 |
| 正常 | 展示四板块、六域状态、四项溯源字段及审计引用 |
| 有限发布 | 顶部持续提示缺失/延迟域，限制结论范围，降低可信度 |
| 今日未生成 | 显示生成状态与原因；可查看标注日期的上一份报告 |
| 生成失败 | 显示失败状态和可追踪错误码，不显示 AI 临时拼接内容 |
| 无数据 | 显示“当前授权范围暂无可验证数据”，四板块不填充通用话术 |
| 无权限 | 展示无权访问，不泄露报告是否存在、来源或摘要 |
| 历史报告 | 显著显示报告日期、当时 `updated_at` 与当前是否已过期 |

移动端保持“结论 → 依据 → 建议 → 溯源”的单列顺序；桌面端可使用目录定位四个板块，但不得用横向排布改变语义顺序。`freshness_status` 和 `confidence` 同时使用文字、图标和辅助说明，满足可访问性要求。

## 11. 可观测性与运营

### 11.1 指标

- 07:00 作业触发、开始、完成与发布时间；
- 各数据域读取成功率、延迟、鲜度状态与合同错误；
- 报告生成耗时、重试次数、发布结果和有限发布原因；
- 候选条目数、因证据/权限/敏感信息/引用失败被拒条目数；
- 按等级聚合的 confidence 分布，不记录经营文本到普通指标标签；
- API 的 `401`、`403`、`404`、`5xx` 与端到端延迟。

### 11.2 告警

- 到运营批准的截止窗口仍无当天 `published` 或合规 `degraded` 报告；
- 任一关键域持续 `missing`、`stale` 或 `conflict`；
- 同一幂等键出现多个发布版本；
- 未授权访问异常增加；
- 输出校验失败率或敏感信息拦截异常增加。

告警阈值和通知对象由现有监控与运营配置管理，不写入前端或 AI Prompt。

## 12. 实施拆分

### 阶段 A：合同与治理确认

1. 锁定六域现有 API、字段、SLA、数据 owner 和授权范围。
2. 审批最低覆盖、关键域、鲜度阈值、可信度规则和有限发布规则。
3. 完成日报响应 Schema、错误码、审计字段和保留策略评审。

### 阶段 B：服务端编排

1. 在不修改受保护系统的前提下实现日报编排适配层。
2. 实现证据标准化、确定性候选、Runtime 调用与质量门禁。
3. 实现报告存储、版本、幂等、历史读取和审计。
4. 接入获准作业平台，先验证人工触发，再审批启用 07:00。

### 阶段 C：Portal 页面

1. 新增 `/daily-report` 与导航入口，不修改 CEO Today 核心组件。
2. 实现四板块、统一卡片、六域状态、历史日期与异常状态。
3. 实现 CEO / 授权管理层路由保护、服务端权限失败处理和审计事件。

### 阶段 D：灰度与上线

1. 使用生产只读数据进行受控验证，禁止 mock/fixture/faker 和静态经营数字。
2. 对比源证据、日报显示与审计引用，完成 CEO 办公室业务验收。
3. 小范围授权管理层灰度，观察鲜度、生成稳定性与误报。
4. 审批后启用每日 07:00，并保留一键停用调度和只读历史访问能力。

## 13. 测试与验收标准

### 13.1 功能验收

- [ ] 独立页面完整包含经营总结、经营机会、经营风险、CEO 行动建议。
- [ ] 每个板块及每张内容卡都按“结论 / 依据 / 建议”呈现。
- [ ] 报告级和条目级均包含 `data_source`、`updated_at`、`freshness_status`、`confidence`。
- [ ] 六域状态可见，实际参与分析的来源与 `data_source` 一致。
- [ ] 每个结论和建议均能追溯到有效 evidence。
- [ ] 07:00 按 `Asia/Shanghai` 触发，同一业务日期重复触发不会重复发布。
- [ ] 历史报告保持当时证据与时间，不被后续生成覆盖。

### 13.2 真实性与降级验收

- [ ] 代码、配置、种子和页面中不存在 mock、fixture、faker 或静态经营数字。
- [ ] 缺字段、无数据、上游失败时不生成替代数字或通用经营结论。
- [ ] `stale`、`missing`、`conflict` 能限制或阻断相关结论并正确降低可信度。
- [ ] 上一份报告不会被冒充为当天报告。
- [ ] AI 输出中的每个事实值与源证据类型、范围、时间、引用一致。

### 13.3 权限与安全验收

- [ ] CEO 和明确授权管理层可查看；其他角色服务端返回拒绝。
- [ ] 撤权立即影响最新与历史报告访问。
- [ ] 用户无法通过修改日期、报告 ID、条目 ID 或追问参数越权。
- [ ] 客户、员工及供应链敏感信息符合最小化与日志脱敏要求。
- [ ] Prompt 注入、无引用内容、禁止动作与敏感输出被质量门禁拦截。

### 13.4 回归边界验收

- [ ] SAP B1、SAP Mirror、Data Core、Auth、AI Runtime 基础配置无变更。
- [ ] CEO Today 核心页面的组件、接口、指标、交互与数据链无变更。
- [ ] V2.0.1 AI 顾问原有问答和权限行为无回归。
- [ ] 日报停用或失败不会影响 CEO Today 与 AI 顾问可用性。

## 14. 上线门禁与回滚

满足以下条件方可上线：六域合同 owner 签字、权限评审通过、真实只读数据验收通过、07:00 幂等演练通过、降级演练通过、安全与隐私评审通过、CEO 办公室确认内容结构。

回滚优先关闭 `ceo_daily_report_0700` 调度与日报导航入口，保留审计和已生成历史记录；不得通过回滚影响 SAP B1、SAP Mirror、Data Core、Auth、AI Runtime 或 CEO Today。报告服务不可用时 Gateway 应返回明确错误，Portal 展示失败关闭状态，不回退到本地报告、缓存经营数字或静态文本。

## 15. 最终交付定义

V2.0.2 的完成标准不是“AI 能生成一段晨报”，而是形成一条具备 **真实数据、证据闭环、鲜度约束、可信度门禁、权限隔离、07:00 幂等生成、失败关闭和完整审计** 的 CEO 经营日报链路。任何不能满足来源、更新时间、鲜度、可信度和证据要求的内容，都不得以正式日报结论发布。
