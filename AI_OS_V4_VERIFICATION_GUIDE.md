# AI OS V4 Verification Guide

Run:

```bash
python verify_production.py
```

The script checks each service health/version response, compares version to `AI-OS-V4.0`, validates runtime status, and reports `PASS` or `FAIL`.
