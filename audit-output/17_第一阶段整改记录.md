# 第一阶段整改记录

## 已完成

### SAP 重复调度

- 保留：`firefox-sap-sync.timer`，每天 22:00。
- 停用：重复的 `/etc/cron.d/firefox-sap-sync`。
- 原 cron 已备份到 `/opt/backups/firefox-sap-sync.cron.20260711-122426.bak`。
- 停用副本保存在 `/etc/cron.disabled/firefox-sap-sync.disabled.20260711-122426`。
- systemd timer 仍为 enabled、active。
- 本次没有触发 SAP 同步，没有连接或修改 SAP。

### 会话安全

- 新登录会话增加 12 小时有效期。
- 会话加入签发时间并校验时钟偏差。
- 旧的无期限会话不再接受，部署后用户需要重新登录。
- 被篡改和过期的会话会被拒绝。

### 写请求来源保护

- POST、PUT、PATCH、DELETE 请求必须来自当前同域页面。
- 校验 Host、Origin/Referer 和反向代理协议。
- 跨域或缺少来源信息的写请求返回 403。
- 增加 `Cross-Origin-Resource-Policy: same-origin`。

## 测试

- 会话签发与读取：通过。
- 会话过期：通过。
- 旧会话拒绝：通过。
- 会话篡改拒绝：通过。
- 同域写请求：通过。
- 跨域及缺失来源写请求：通过。
- Python 语法检查：通过。
- Git diff 格式检查：通过。

## 尚未完成

- 生产服务器访问 GitHub 超时，服务器 Git 工作副本尚未更新。
- 代码安全修复需在分支推送、生产备份和部署验证后生效。
- 数据库外键、六类主档、知识 AI 管道和公网端口治理需要后续独立阶段处理。

