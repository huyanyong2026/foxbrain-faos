# VAFOX Huyan Portal V1.4 Business Analysis Report

## 1. 版本结论

V1.4 在 **VAFOX Huyan Portal V1.3 稳定版本**上新增一级页面“经营分析”，不重建门户，也不改变 V1.3 的视觉语言、权限边界或数据事实链。页面面向董事、CEO 与既有 Auth 授权的管理层，保持只读。

本报告同时是产品范围、数据接入约束和验收基线。只有真实生产 API 合同、权限校验、数据来源与更新时间均通过验收后，V1.4 才可标记为可发布；接口缺失时界面必须显示“待补齐”，不得以示例数字代替。

## 2. 受保护基线

下列能力全部保持 V1.3 原状，不在 V1.4 变更范围内：

| 受保护项 | V1.4 约束 |
| --- | --- |
| CEO Today | 页面结构、交互和业务含义不变 |
| `GET /api/ceo/today` | 请求路径、响应合同与现有调用不变 |
| SAP B1 | 不修改、不直连、不写回 |
| SAP Mirror | 不修改同步、表结构或任务 |
| Data Core | 不修改数据模型、指标算法或服务实现 |
| Auth | 复用现有会话、角色、数据范围与审计，不新增旁路 |
| AI Runtime | 不修改模型、提示词、路由或运行时 |

V1.4 的浏览器数据流保持为：

`Browser → Gateway（既有 Auth）→ Core API → Data Core`。

浏览器不得连接 SAP B1、SAP Mirror、数据库或内部服务地址。经营分析与 CEO Today 使用相互独立的请求和状态；经营分析接口失败不得影响 CEO Today。

## 3. 页面信息架构

“经营分析”沿用 V1.3 左侧导航、白色/浅灰画布、VAFOX 橙色强调、卡片层级、字号、间距、圆角、空状态与移动端适配。页面由一个公共筛选区和五个独立分析区组成：

1. 整体销售分析
2. 店铺销售分析
3. 员工销售分析
4. 顾客购买分析
5. 供应商销售分析

公共筛选条件仅可发送真实 API 已支持的参数。推荐合同为 `date_from`、`date_to`、`compare_from`、`compare_to`、`store_code`；若服务端未声明某参数，前端不得自行模拟筛选结果。切换分析区时保留合法的日期与门店范围，并分别展示数据来源、数据截止时间、鲜度和错误状态。

## 4. 数据源矩阵

| 分析区 | 唯一事实来源 | 展示内容 | 缺失处理 |
| --- | --- | --- | --- |
| 整体销售 | `GET /api/core/sales` | 销售、订单、客单价、趋势 | “销售数据待补齐” |
| 店铺销售 | `GET /api/core/sales` 的门店维度 | 振兴、南山、航苑、金沙、网店的销售表现与趋势 | 对缺失门店逐店显示“待补齐” |
| 员工销售 | `GET /api/core/members` | API 已返回且获授权的员工销售指标 | 字段或记录缺失均显示“待补齐” |
| 顾客购买 | Customer360 的既有 Gateway/Core API 合同 | 客户价值、购买行为、机会 | 合同或字段缺失显示“待补齐” |
| 供应商销售 | 独立供应链数据域的既有 Gateway API 合同 | 获授权的供应商销售/供货贡献事实 | 合同或字段缺失显示“待补齐” |

Customer360 与供应链接口路径必须由各自现有服务合同提供。本需求没有给出这两个路径，因此 V1.4 不臆造 URL、不回退到 CEO Today，也不把客户记录转换成供应商记录。发布前应由接口负责人确认路径、字段、权限和鲜度语义。

每个成功响应至少应能提供：业务数据、数据来源、更新时间/数据截止时间。缺少溯源元数据时可以展示已返回的业务事实，但必须显著标记“数据溯源待补齐”，不得声称“实时”或“已更新”。

## 5. 五个分析区

### 5.1 整体销售分析

- **销售**：直接展示 `/api/core/sales` 返回的销售额及币种/口径；不在前端拼接其他接口的金额。
- **订单**：直接展示 API 返回的有效订单数及其统计口径。
- **客单价**：优先展示 API 返回值。只有销售额与订单数属于同一响应、同一周期、同一范围且订单数大于零时，前端才可显示 `销售额 ÷ 订单数`，并标注公式；否则显示“待补齐”。
- **趋势**：使用 API 返回的时间序列和比较口径绘制，不补点、不外推。序列缺日时保留断点并提示“该日数据待补齐”。
- 金额格式采用人民币格式化，但不得改变 API 数值精度后再参与计算。

加载、无数据、接口错误和无权限必须是四种不同状态。HTTP 成功但结果为空显示“当前范围暂无销售数据”；网络/服务错误显示“销售数据暂不可用”；`401/403` 显示“无权查看该经营范围”。

### 5.2 店铺销售分析

门店展示顺序固定为：**振兴、南山、航苑、金沙、网店**。前端以 Core 返回的稳定 `store_code` 映射门店，不依赖可变的显示名称做聚合。

每店卡片展示 API 确实返回的销售、订单、客单价与趋势。某店缺少响应记录时仍保留该店位置并显示“待补齐”，不得将其解释为零销售；零值只有在 API 明确返回数值 `0` 时才能显示。五店合计仅使用 API 的整体汇总，或在 API 明确声明五店明细口径一致且完整时计算，不得在缺店情况下给出伪完整合计。

### 5.3 员工销售分析

员工区只读取 `GET /api/core/members`，并只展示响应中获授权的员工/成员销售字段。建议字段包括员工标识、显示名、所属门店、销售额、订单数、客单价、趋势与统计周期，但具体字段名以真实合同为准。

- 未返回销售字段：对应单元格显示“待补齐”。
- 返回员工但无统计记录：显示“销售数据待补齐”，不得显示 `0`。
- 明确返回零值：显示 `0`。
- 不在前端推断业绩归属，不把员工排序包装成绩效评价，不暴露未授权客户明细。

### 5.4 顾客购买分析

顾客区以 Customer360 为唯一客户事实域，分为：

- **客户价值**：仅展示 Customer360 已定义的价值、分层、累计贡献或生命周期字段。
- **购买行为**：展示已授权的购买频次、最近购买、品类/品牌偏好等聚合事实。
- **机会**：展示 Customer360 返回的机会、原因、优先级与建议动作，并保留事实来源。

页面默认采用聚合或最小化信息；客户身份信息继续受现有 Auth 数据范围控制。不得根据缺失字段自行给客户打标签，不得把 AI 推断显示成 Customer360 事实。

### 5.5 供应商销售分析

供应商区只接入**独立供应链数据域**。其接口、类型、请求状态和展示组件应与 Customer360 分离：

- 不使用客户 ID 查询供应商；
- 不复用 Customer360 响应作为供应商数据；
- 不把“购买客户”与“供货供应商”放入同一数组、排行榜或统计口径；
- 不用客户机会填充供应商机会；
- 联合分析必须由供应链域提供明确、已治理的聚合合同，前端不得自行关联。

在供应链域尚未提供确认过的真实 API 合同时，本区显示“供应链数据待补齐”，并保留数据域说明；不得生成供应商名称、金额或排行。

## 6. 真实数据与可信状态

V1.4 禁止任何 `mock`、`fixture`、`faker`、演示 JSON、硬编码经营金额、随机数、本地业务快照或静态成功回退。门店名称和页面文案属于界面配置，不属于业务事实；除这类配置外，所有金额、数量、趋势、客户、员工和供应商内容必须来自上述真实 API。

可信状态规则：

| 状态 | UI 表达 | 禁止行为 |
| --- | --- | --- |
| 加载中 | 骨架/“正在读取…” | 先显示上次硬编码值 |
| 成功有数据 | 展示事实及溯源 | 标注未经合同支持的“实时” |
| 成功无数据 | “当前范围暂无数据” | 将空数组伪装为接口错误 |
| 部分缺失 | 缺失项“待补齐” | 用 `0`、`—` 或估算值掩盖缺失语义 |
| API 错误 | “数据暂不可用”，提供重试 | 回退到 mock 或其他数据域 |
| 无权限 | “无权查看该经营范围” | 重试绕过 Auth 或展示缓存明细 |

日志与前端错误信息不得包含访问令牌、客户 PII、SAP 连接信息或完整原始响应。

## 7. API 与前端验收

### 7.1 合同验收

- `/api/core/sales` 能按获授权范围返回整体与门店销售事实、趋势口径和溯源字段。
- `/api/core/members` 能区分零值与字段缺失，且只返回当前用户可见成员。
- Customer360 负责人提供现有真实 Gateway 路径、字段定义、PII 分级和机会口径。
- 供应链负责人提供独立供应商域的现有真实 Gateway 路径、字段定义和权限范围。
- 所有接口沿用 Auth；`401/403`、空结果、部分结果和服务错误具有可区分语义。

### 7.2 自动化门禁

1. 静态检查确认 CEO Today 仍且仅按 V1.3 调用 `gatewayFetch("/api/ceo/today")`。
2. 静态检查确认经营分析销售与成员请求分别使用 `/api/core/sales`、`/api/core/members`。
3. 扫描 Huyan 运行时代码和业务资源，拒绝 `mock`、`fixture`、`faker`、随机业务数据和静态业务 JSON。
4. 合同测试覆盖有效响应、空响应、部分字段、零值、`401`、`403`、`5xx` 与超时。
5. 组件测试确认五店顺序固定、缺店显示“待补齐”、零值不被误判为缺失。
6. 隔离测试确认 Customer360 数据不会进入供应商组件，供应链响应不会进入客户组件。
7. 回归测试确认 SAP B1、SAP Mirror、Data Core、Auth、AI Runtime 和 `/api/ceo/today` 无变更。
8. TypeScript、构建、桌面/移动端可访问性与视觉回归检查通过。

### 7.3 人工验收

- 在浏览器 Network 中确认所有业务请求经 Gateway 发出，不存在数据库/SAP 直连。
- 用有权限与无权限账号分别验证组织、门店、员工和客户范围。
- 对照 API 原始响应核验销售、订单、客单价、趋势和五店数值。
- 断开每个数据域，确认只影响相应分析区且从不出现替代业务数字。
- 检查窄屏下导航、筛选器、趋势图、表格与“待补齐”状态可读。

## 8. 发布与回滚

V1.4 仅发布 Huyan 前端经营分析页面及其前端合同/测试，不携带受保护系统变更。灰度前保存 V1.3 前端制品；若出现口径不一致、越权、客户/供应商混域、无溯源业务数字或任何模拟数据，应立即停止推广并回滚 Huyan 前端至 V1.3。回滚不得修改或回退 SAP B1、SAP Mirror、Data Core、Auth、AI Runtime。

## 9. 完成定义

V1.4 完成必须同时满足：五个分析区可访问；销售与成员使用指定真实 API；Customer360 与供应商域使用各自经确认的真实合同；所有缺失内容明确显示“待补齐”；客户和供应商严格隔离；V1.3 视觉规范与 CEO Today 完整保持；自动化及人工验收全部通过；仓库和生产制品中不存在任何模拟经营数据。

在 Customer360 与独立供应链 API 合同未确认前，相关区域可以以可信“待补齐”状态随页面交付，但不得宣称这两个数据域已经完成生产绑定。
