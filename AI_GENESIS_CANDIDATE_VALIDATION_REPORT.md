# AI Genesis Candidate Validation Report

Status: **Template / generated by `scripts/ai_genesis_candidate_build.sh` at execution time**

Target: `ai.vafox.com`  
Current production pointer requirement: `current-enterprise-ai -> releases/fba3c17`  
Release mode: **Release Candidate only**

## Guardrails

- No production cutover is authorized.
- No `current-enterprise-ai` symlink change is authorized.
- No data migration is authorized.
- Validation must target only the generated candidate release directory and preview runtime.

## Execution

Run the candidate-only build executor from a clean repository checkout on the target host:

```bash
scripts/ai_genesis_candidate_build.sh
```

The script overwrites this file with the concrete candidate ID, commit, release directory, smoke-test matrix, evidence paths, and overall validation status.

## Required Validation Areas

| Area | Required coverage |
|---|---|
| Login | Preview login page or redirect path responds from candidate runtime. |
| Auth | Unauthenticated identity endpoint rejects or redirects safely. |
| Agents | Agents surface responds from candidate runtime. |
| Knowledge | Knowledge surface responds from candidate runtime. |
| Workflows | Workflow/task surface responds from candidate runtime. |
| WeCom | WeCom surface responds in dry-run/sandbox candidate mode. |
| API | Candidate API/version health endpoint responds. |
| Nginx preview | Preview route/config artifact exists and preview root responds. |

## Cleanup

The executor stops the preview process automatically unless `KEEP_RUNTIME=1` is set. On failure, runtime scratch data is removed unless `KEEP_FAILED_RELEASE=1` is set. The script validates the production symlink both before and after candidate execution.
