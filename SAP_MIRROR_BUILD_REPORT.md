# SAP Mirror Engine 全库同步施工报告

## 完成结论

- 完成时间：2026-07-13 16:00（Asia/Shanghai）
- SAP 来源：独立 SAP B1 公司数据库，只读访问
- Mirror 目标：`core.vafox.com` 上的 `SAP_MIRROR`
- 来源业务表：2,120 张
- Mirror 业务表：2,120 张
- 缺失表：0
- 多余表：0
- 行数不一致表：0
- 最终同步批次：`completed`，2,120 成功，0 失败

本次完成的是 SAP B1 公司数据库业务表的逻辑镜像，包括表结构与数据。它不是 SQL Server 实例级物理克隆，不复制登录账号、SQL Agent 作业、服务器配置或 SAP 生产环境程序。

## 已复制与未复制

- 已复制：来源公司库发现的全部 2,120 张业务表。
- 未复制：0 张来源业务表。
- 特殊旧字段：`OCPC`、`OFRM`、`OHMM`、`OULA` 的旧式文本内容以可逆二进制方式保存，避免旧 SAP 数据中的非标准编码丢失。
- 大表支持：复合主键断点续传、批量写入、主键缺口扫描和失败恢复。

## 最终对账

独立对账没有复用同步任务的成功状态，而是重新查询来源与 Mirror。

| 指标 | SAP 来源 | Mirror | 结果 |
| --- | ---: | ---: | --- |
| 业务表数量 | 2,120 | 2,120 | 通过 |
| 商品数量 | 96,009 | 96,009 | 通过 |
| 库存数量 | 42,447 | 42,447 | 通过 |
| 客户数量 | 22 | 22 | 通过 |
| 供应商数量 | 121 | 121 | 通过 |
| 有效销售单据 | 10,334 | 10,334 | 通过 |
| 销售金额 | 35,099,547.70 | 35,099,547.70 | 通过 |
| 有效采购单据 | 1,180 | 1,180 | 通过 |
| 采购金额 | 27,064,122.86 | 27,064,122.86 | 通过 |

库存字段级校准发现 60 个商品与仓库组合在营业期间发生变化，已按来源主键更新 Mirror。最终库存数量和行数均一致。

最终机器可读对账文件：

`/opt/foxbrain-core/logs/SAP_MIRROR_RECONCILIATION_FINAL_20260713.json`

## 安全边界

- 来源账号：专用只读账号。
- 实测权限：SELECT=1，INSERT=0，UPDATE=0，DELETE=0，ALTER=0。
- 未修改 SAP 数据，未修改 SAP 数据库结构，未在 SAP 服务器安装程序。
- Mirror SQL Server 仅监听 `127.0.0.1:11433`，数据库端口不向公网开放。
- 连接密钥只保存在服务器环境变量文件中，未写入 Git。
- 自动定时任务仍为关闭状态，`foxbrain-sap-mirror.timer` 未启用。

## 同步与恢复机制

- 同步服务：`foxbrain-sap-mirror.service`
- 状态数据库：`/opt/foxbrain-core/sync/mirror-state.db`
- 结构化日志：`/opt/foxbrain-core/logs/sap-mirror.jsonl`
- 每表保存状态、来源行数、目标行数、已复制行数、更新时间和错误。
- 有主键的大表使用 keyset 分页和断点续传。
- 失败表和中断表优先恢复，再执行全库核验。
- 来源营业中新增数据时，按结束时来源行数对账，并扫描主键缺口。

## 备份

- 首次施工前备份：`/opt/foxbrain-core/backup/mirror-20260713-085731/`
- 全量完成后数据库备份：`/opt/foxbrain-core/backup/SAP_MIRROR_FULL_20260713-1605.bak`（185MB）。
- 数据库备份已通过 `RESTORE VERIFYONLY WITH CHECKSUM`，SHA-256 校验文件与备份同目录保存。
- 多阶段状态备份：`/opt/foxbrain-core/backup/mirror-state-before-*.db`
- 最终状态整理前备份：`/opt/foxbrain-core/backup/mirror-state-before-run-cleanup-20260713.db`
- 每次同步引擎升级前均保存上一版本脚本到 `/opt/foxbrain-core/backup/`。

## 测试结果

- 同步引擎单元测试：12 项通过。
- Python 编译检查：通过。
- 只读权限复核：通过。
- 2,120 张表存在性与行数对账：通过。
- 商品、库存、客户、供应商、销售、采购指标对账：全部通过。
- 服务最终状态：正常完成后退出，无并发重复进程。

## 已知边界与下一步

1. 本次是已验证的全量逻辑镜像快照。SAP 在营业中持续变化，后续数据新鲜度依赖增量同步。
2. 自动定时任务尚未批准启用，当前不会自动运行。
3. 下一阶段应把 `updateDate`、主键缺口和字段级差异校准固化为正式增量策略，并为销售、库存、采购分别设置水位与对账。
4. 定时任务启用前，先连续执行数日人工同步和对账，确认运行窗口、耗时和生产负载。
5. Dashboard 应展示最后成功时间、2,120 张表状态、核心指标对账和数据陈旧提醒。
