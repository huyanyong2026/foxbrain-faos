# AI OS V4 Version Management

The canonical version is `AI-OS-V4.0`. Override with `FOXBRAIN_VERSION` only for release promotion. Commit is read from `GIT_COMMIT`, `COMMIT_SHA`, deployment metadata, or local git fallback.
