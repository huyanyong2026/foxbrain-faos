# VAFOX V6.1.10 CEO Dashboard Integration Report

> **发布目标：** `huyan.vafox.com` CEO 经营驾驶舱
> **版本：** VAFOX V6.1.10
> **交付类型：** 现有能力统一入口整合（不重新开发业务模块）
> **目标状态：** CEO OS Production Ready
> **报告日期：** 2026-08-02
> **变更边界：** SAP / Data Core / Auth 保持不变

---

## 1. 执行结论

VAFOX V6.1.10 采用“**一个入口、五个分析域、一个 AI 顾问入口、三条底座不变**”的整合方案：

- 唯一入口为 `https://huyan.vafox.com/`，首页命名为 **CEO Today**。
- 复用已有销售、店铺、产品、库存、供应商、员工、客户、AI 日报与 System Guardian 能力，不复制页面、不重写算法、不新建业务事实源。
- 经营分析统一容纳整体销售、店铺销售、员工销售、顾客购买与供应商分析。
- 商品、库存、客户各自形成一个 CEO 分析域，原有专业页面作为下钻目标继续存在。
- AI 日报、Copilot、CEO Agent、决策与证据能力合并为一个 **AI 顾问**入口；AI 只解释、建议与生成决策草案，不直接执行。
- SAP 继续作为业务事实层，Data Core 继续作为企业数字孪生与指标语义层，Gateway/Auth 继续负责身份、会话和授权。

**发布判定：有条件 GO。** 信息架构、复用映射、权限边界、降级规则、验收矩阵和回滚边界已经明确；生产切换必须在第 11 节所有 P0 门禁取得证据后执行。未取得证据时不得把“设计就绪”表述为“生产已上线”。

---

## 2. 范围与非目标

### 2.1 本次范围

1. 为 CEO 建立统一入口和统一导航。
2. 把现有模块按 CEO 决策语义重新编排，而不是按后台系统名称堆叠。
3. 统一时间范围、组织范围、数据新鲜度、口径说明、来源与下钻行为。
4. 为异常、空数据、超时、无权限和数据过期定义一致状态。
5. 固化生产验收、观测、回滚与责任边界。

### 2.2 明确非目标

- 不修改 SAP 表、接口、同步作业或业务逻辑。
- 不创建第二套 Data Core、指标仓库或客户主数据。
- 不创建第二套登录、CEO 用户表、角色表、Token 或权限判断。
- 不重写销售、店铺、员工、商品、库存、供应商、客户、AI 日报或 Guardian 模块。
- 不把 Huyan 变成 SAP 操作台、数据库查询器或基础设施控制台。
- 不允许 AI 绕过审批执行调价、调拨、采购、触客、排班或系统修复。

### 2.3 整合定义

本报告中的“整合”仅包含：**入口聚合、导航编排、只读摘要、筛选上下文传递、深链接、权限继承、状态归一、证据关联与统一观测**。业务计算与事实仍由原模块负责。

---

## 3. 现有能力复用清单

| 已有能力 | CEO 驾驶舱归属 | 复用方式 | 禁止事项 |
| --- | --- | --- | --- |
| 销售 | CEO Today、经营分析 | 读取现有销售摘要、趋势和利润视图；下钻原页面 | 不在前端重算销售额或利润 |
| 店铺 | 经营分析 | 复用店铺中心、店铺经营、Store Intelligence | 不复制店铺档案 |
| 员工 | 经营分析 | 复用员工中心、HR/员工销售贡献 | 不改变绩效定义和可见范围 |
| 客户 | CEO Today、经营分析、客户分析 | 复用客户增长、会员/客户档案和购买记录 | 不导出或扩散个人明细 |
| 供应商 | 经营分析 | 复用供应商中心中的合同、采购、付款和评估 | 不新建供应商主数据 |
| 产品 | 商品分析 | 复用产品中心、品牌/商品智能 | 不复制 SKU、商品知识或口径 |
| 库存 | CEO Today、库存分析 | 复用库存中心、库存风险、库存决策与智能分析 | 不直接创建调拨/采购单 |
| AI 日报 | CEO Today、AI 顾问 | 复用 Daily Intelligence、CEO Morning Brief 与报告证据 | 不生成无来源的确定性结论 |
| System Guardian | CEO Today、全局状态 | 复用系统健康、业务健康、数据新鲜度和风险中心 | 不提供启停、修复或配置按钮 |

复用原则：摘要卡只保留 CEO 做判断所需的最小字段；专业明细通过原模块深链接打开。相同指标在不同页面必须共享 `metric_id`、统计口径、时区、数据截止时间和来源引用。

---

## 4. 目标信息架构

### 4.1 一级导航

```text
CEO Today
经营分析
  ├─ 整体销售
  ├─ 店铺销售
  ├─ 员工销售
  ├─ 顾客购买
  └─ 供应商分析
商品分析
库存分析
客户分析
AI 顾问
```

System Guardian 不作为分散的业务菜单项重复出现：它在全局顶栏显示整体健康，在 CEO Today 显示关键风险，并从状态入口下钻原有系统健康与风险页面。

### 4.2 建议统一路由契约

下表定义 Huyan 的稳定入口别名；别名只做编排或重定向，原有路由不删除。

| 导航项 | Huyan 稳定入口 | 首选现有下钻目标 |
| --- | --- | --- |
| CEO Today | `/` | `/ceo-home`、`/daily-intelligence`、`/risks` |
| 经营分析 | `/business-analysis` | `/business-overview`、`/finance` |
| 整体销售 | `/business-analysis/sales` | `/overview`、`/finance` |
| 店铺销售 | `/business-analysis/stores` | `/stores`、`/store-intelligence`、`/stores/operations` |
| 员工销售 | `/business-analysis/employees` | `/employees`、`/hr` |
| 顾客购买 | `/business-analysis/customer-purchases` | `/customer-growth`、`/members` |
| 供应商分析 | `/business-analysis/suppliers` | `/suppliers` |
| 商品分析 | `/product-analysis` | `/products`、`/brand-intelligence` |
| 库存分析 | `/inventory-analysis` | `/inventory`、`/inventory-intelligence`、`/inventory/risk`、`/inventory-decision` |
| 客户分析 | `/customer-analysis` | `/customer-growth`、`/members` |
| AI 顾问 | `/ai-advisor` | `/copilot`、`/daily-intelligence`、`/ceo-decision-center`、`/evidence-chain` |
| Guardian 状态 | `/guardian` | `/system/health`、`/business-health`、`/system/data-readiness`、`/risks` |

所有别名应保留查询上下文：`period`、`org_scope`、`store_id`、`brand_id`、`category_id` 和 `metric_id`。禁止用 URL 传递姓名、手机号等敏感字段。

---

## 5. 首页：CEO Today

CEO Today 回答四个问题：**今天发生了什么、为什么重要、需要关注什么、CEO 要决定什么。** 默认展示最新已完成业务日，并清楚标注数据截止时间；不以浏览器当前时间假装数据已更新。

### 5.1 页面顺序

1. **全局状态条**：当前组织、业务日期、数据截止时间、SAP→Core→AI→Huyan 链路、Guardian 状态、未读风险。
2. **核心经营脉搏**：销售额、毛利/利润质量、库存风险、客户机会；每张卡包含本期值、对比值、目标差、更新时间和来源。
3. **今日变化**：由 AI 日报复用“发生了什么 → 原因 → 影响”的结构，事实与推断分区。
4. **风险与机会**：统一优先级队列，按影响、紧迫性、置信度和数据新鲜度排序。
5. **待 CEO 决策**：问题、选项、推荐、预期影响、风险、证据和有效期；默认不执行。
6. **分域概览**：经营、商品、库存、客户四个入口，显示摘要与异常数量。

### 5.2 首页卡片最小契约

```json
{
  "id": "stable-card-id",
  "title": "指标或事项名称",
  "value": "display-ready value",
  "comparison": "同比、环比或目标差",
  "status": "OK|ATTENTION|RISK|UNKNOWN",
  "as_of": "ISO-8601 timestamp",
  "business_date": "YYYY-MM-DD",
  "source": "existing module / metric id",
  "freshness": "FRESH|DELAYED|STALE|UNKNOWN",
  "scope": "authorized aggregate scope",
  "evidence_ref": "auditable reference",
  "drilldown": "authorized existing route"
}
```

`0`、无数据、未授权、加载失败和未同步必须是五种不同状态，不能统一显示为 `0` 或 `—`。

---

## 6. 分析域整合

### 6.1 经营分析

经营分析使用同一筛选栏（业务日期、对比周期、组织/店铺、品牌、品类），并包含五个现有能力视图：

| 视图 | CEO 核心问题 | 复用指标/内容 | 下钻 |
| --- | --- | --- | --- |
| 整体销售 | 企业是否达成目标，增长质量如何？ | 销售、毛利、客单、件单、同比/环比、目标达成、利润构成 | 原销售/财务明细 |
| 店铺销售 | 哪些店驱动增长，哪些店偏离？ | 店铺排名、贡献、趋势、客单、转化、异常 | 店铺中心与 Store Intelligence |
| 员工销售 | 贡献是否健康，是否存在组织风险？ | 授权范围内的员工销售贡献、目标达成、趋势、团队分布 | 员工中心与 HR |
| 顾客购买 | 谁在买、买什么、是否复购？ | 客群、复购、购买品类、客单、生命周期；首页仅聚合 | 客户增长与授权客户档案 |
| 供应商分析 | 供应是否支撑经营目标？ | 采购、到货/履约、付款、供货风险、关联库存 | 供应商中心 |

跨视图联动只能传递 ID 和筛选条件；各模块仍对数据范围做服务端授权。员工和顾客明细不得因为 CEO 聚合页而降低字段级保护。

### 6.2 商品分析

统一展示品牌/品类/SKU 的销售贡献、毛利表现、趋势、生命周期、知识资产状态和关联库存风险。商品分析复用产品中心与 Product/Brand Intelligence：

- 默认按贡献与异常展示，不把完整 SKU 表直接铺在首页。
- 商品事实、AI 商品解释和销售建议明确分层。
- 点击商品进入原产品档案；图片、规格、知识引用、销售历史均以原模块为准。
- “机会商品”必须同时显示库存可售性和数据截止时间，避免只按销量建议。

### 6.3 库存分析

统一展示库存金额/数量、周转、库龄、缺货、滞销、尺码/结构风险和店间差异，复用 Inventory、Inventory Intelligence、Risk 与 Decision 页面。

- 风险卡必须显示店铺、SKU/品类、严重度、影响、数据时间和建议核查动作。
- 建议调拨、补货或清理仅形成决策草案；提交后仍进入既有审批/业务工作流。
- SAP/Core 数据延迟时，隐藏任何看似可执行的数量建议，并显示 `STALE` 或 `UNKNOWN`。
- 不在 Huyan 写库存，不创建影子库存台账。

### 6.4 客户分析

统一展示客户规模、新增/活跃/沉睡、复购、价值分层、品类偏好、购买路径和授权机会，复用 Customer Growth、客户/会员中心与购买历史。

- CEO Today 与经营分析只展示聚合数据。
- 客户级下钻必须再次执行服务端权限与组织范围校验。
- 手机号、地址、联系凭据等默认脱敏；导出、分享和触客继续沿用原策略。
- AI 推荐必须标注建议性质、证据与目的，不得自动发送营销消息。

---

## 7. AI 顾问统一入口

AI 顾问不是新的模型或 Agent，而是现有 AI 日报、Copilot、CEO Agent、决策中心与证据链的统一会话入口。

### 7.1 入口能力

- **今日问答**：基于 CEO Today 当前时间与组织上下文提问。
- **经营诊断**：调用现有销售、店铺、员工、客户、供应商分析结果。
- **商品/库存/客户专题**：从当前分析页携带非敏感筛选上下文进入。
- **日报与复盘**：读取现有 AI 日报和历史已批准报告。
- **决策草案**：生成问题、方案、风险、建议、证据与有效期，交由人审批。

### 7.2 每个回答的强制输出

1. 结论摘要。
2. 已验证事实。
3. AI 推断及置信度。
4. 数据范围与截止时间。
5. 来源/证据链接。
6. 局限、冲突或缺失数据。
7. 建议的人工下一步。

没有证据的回答必须明确说“不足以判断”；不得补造实时数据。任何有业务影响的动作都必须保持 `建议 → 人工审批 → 既有工作流执行 → 结果回写`。

---

## 8. SAP / Data Core / Auth 不变承诺

### 8.1 数据链路

```text
SAP（Business Truth）
  → 既有同步/治理
Data Core（Digital Twin + Metric Semantics）
  → 既有只读数据产品/API
AI（Analysis + Recommendation）
  → Gateway（Identity + Policy + Audit）
Huyan（CEO Presentation + Decision Entry）
```

Huyan 不直连 SAP 或数据库，不持有业务主数据，不把浏览器计算结果写回事实层。Data Core 的实体 ID、指标口径、血缘、质量与新鲜度字段原样继承。

### 8.2 身份与授权

- 继续使用 Gateway/Auth 的现有会话与登录跳转，不在 Huyan 创建登录逻辑。
- 继续执行既有 RBAC + ABAC + 数据范围策略；导航可见不等于数据授权。
- API 必须服务端鉴权，前端隐藏菜单不能作为安全控制。
- CEO 私有上下文、员工 AI、客户数据与 Core 事实域继续隔离。
- 登录、拒绝、敏感下钻、AI 问答、证据读取、审批和导出均写入既有审计链。

---

## 9. 状态、性能与可观测性

### 9.1 统一降级

| 场景 | 用户体验 | 禁止行为 |
| --- | --- | --- |
| 单模块超时 | 该卡片显示超时和最近有效时间，其他模块继续服务 | 用演示值冒充实时值 |
| SAP/Core 延迟 | 显示延迟范围、最后成功业务日期和受影响指标 | 给出精确执行数量 |
| AI 不可用 | 保留事实卡和原模块下钻，AI 区显示不可用 | 阻断整个驾驶舱 |
| Guardian 异常 | 顶栏与风险区提升严重度并给出只读详情 | 在 CEO 页直接修复系统 |
| 无权限 | 显示无权限或不渲染敏感模块，并记录拒绝 | 返回敏感字段后由前端遮挡 |
| 指标冲突 | 标记冲突来源与口径，暂停合并结论 | 任意选择更“好看”的数值 |

### 9.2 SLO 建议

- 月度入口可用性：≥ 99.9%（不含已公告维护窗）。
- 首页聚合 API：P95 ≤ 2.0 秒；首屏可先返回已批准缓存快照。
- 专业下钻：P95 ≤ 3.0 秒，以原模块现行 SLO 更严格者为准。
- 错误率：5xx < 0.5%；权限拒绝单独统计，不计入系统错误。
- 每张关键卡的数据新鲜度覆盖率：100%；不得缺少 `as_of`。

### 9.3 必备观测

统一关联 `request_id`、`user/VID`（日志中按策略脱敏）、`role`、`scope`、`route`、`source_module`、`metric_id`、`business_date`、`as_of`、`freshness`、`latency`、`result` 和 `evidence_ref`。告警至少覆盖入口不可用、聚合 API 错误率、认证失败突增、数据过期、指标冲突、AI 无证据回答和审计写入失败。

---

## 10. 实施与切换计划（仅整合，不重开发）

### Phase 0 — 冻结边界

- 固化现有模块、路由、API、权限和指标清单。
- 对每张 CEO 卡绑定唯一来源、负责人、口径和新鲜度阈值。
- 禁止在整合期顺带重构 SAP、Core、Auth 或业务算法。

### Phase 1 — 入口编排

- 将 `/` 作为 CEO Today；配置一级导航与稳定别名。
- 摘要卡通过现有 API/聚合契约读取，所有专业详情深链接回原模块。
- System Guardian 状态进入顶栏和风险区。

### Phase 2 — 上下文与 AI 统一

- 对时间、组织、店铺、品牌、品类和指标使用统一上下文键。
- 将 AI 日报、问答、决策和证据入口收敛到 AI 顾问。
- 验证跨页上下文不扩大授权范围。

### Phase 3 — 生产门禁

- 在预生产完成数据对账、角色矩阵、异常降级、性能、安全、审计和回滚演练。
- 使用 CEO 测试账号完成桌面端与移动端关键旅程验收。
- 只切换 Huyan 入口/导航配置；不迁移业务数据。

### Phase 4 — 灰度与稳定

- 先对受控 CEO/管理员账号灰度，再扩大到批准的高管范围。
- 观察至少一个完整业务日和一次 SAP/Core 日切。
- 指标冲突、审计失败或权限泄漏立即回滚入口编排。

---

## 11. Production Ready 验收矩阵

### 11.1 P0 发布门禁

| 编号 | 验收项 | 通过标准 | 证据负责人 |
| --- | --- | --- | --- |
| P0-01 | 唯一入口 | `huyan.vafox.com/` 登录后进入 CEO Today；未登录走既有 Gateway/Auth | Web + Auth |
| P0-02 | 六类导航 | 经营、商品、库存、客户、AI 顾问及首页所有链接无 404/循环跳转 | Web |
| P0-03 | 模块复用 | 每个摘要可追溯到现有模块/API；无影子业务数据 | Product + Data |
| P0-04 | 销售对账 | 整体/店铺/员工/顾客/供应商视图与来源模块在相同范围、时点和口径一致 | Data Owners |
| P0-05 | SAP/Core 不变 | 无 schema、同步、业务逻辑或主数据写入变更 | SAP + Core |
| P0-06 | Auth 不变 | 会话、角色、ABAC、数据范围、退出和超时行为与既有策略一致 | Security |
| P0-07 | 敏感数据 | 非授权角色无法通过 UI、API、深链接或 AI 获得员工/客户敏感数据 | Security |
| P0-08 | AI 治理 | 回答具备来源、时点、范围、置信度/局限；动作必须人工审批 | AI Owner |
| P0-09 | 数据新鲜度 | 所有关键指标显示业务日期、`as_of` 和 freshness；延迟可见 | Data Ops |
| P0-10 | 降级隔离 | Core/AI/单模块故障不会生成假数据，也不会拖垮整个首页 | SRE |
| P0-11 | 审计 | 登录、读取、拒绝、AI、证据、审批/导出事件端到端可检索 | Security + SRE |
| P0-12 | 回滚 | 在约定 RTO 内恢复旧入口，且无需回滚 SAP/Core/Auth 或业务数据 | Release Manager |

### 11.2 关键旅程

1. CEO 登录 → CEO Today → 查看截止时间 → 下钻整体销售 → 返回时保留周期。
2. CEO 从店铺异常 → 店铺销售 → 商品 → 库存风险，范围保持一致。
3. CEO 从客户聚合 → 授权客户下钻；无权限用户同一路径被服务端拒绝。
4. CEO 从风险卡 → Guardian 只读详情 → 回到待决事项。
5. CEO 在任一分析页进入 AI 顾问 → 获取带证据回答 → 生成决策草案 → 人工审批。
6. 模拟 Core 延迟、AI 超时、Guardian 异常和部分 API 失败，验证状态准确且互相隔离。
7. 登出/会话过期 → 原请求不泄露页面数据 → 走既有认证入口。

### 11.3 发布判定规则

- 任一 P0 未通过：**NO-GO**。
- P0 全部通过且无未接受的 Critical/High 安全问题：**GO**。
- P1 体验问题可在责任人、期限和缓解措施明确后接受，不得降级 P0。
- 生产批准必须附测试时间、环境、数据截止时间、证据链接与批准人；本报告本身不替代生产证据。

---

## 12. 回滚方案

回滚单位是 **Huyan 入口与导航编排**，不是业务模块：

1. 将 `huyan.vafox.com` 恢复到上一个已验证 Web 版本/路由配置。
2. 保留原模块路由和 API，用户可继续通过既有入口工作。
3. 清除或旁路新聚合缓存，禁止删除 SAP/Core 事实和审计记录。
4. 验证 Gateway 登录、旧首页、关键模块、健康端点和审计写入。
5. 记录触发原因、影响时段、关联 ID 和恢复时间，再决定重新灰度。

建议目标：入口配置回滚 RTO ≤ 15 分钟，RPO = 0（本次整合不产生新的业务事实）。

---

## 13. RACI

| 事项 | A（最终负责） | R（执行） | C（协作） |
| --- | --- | --- | --- |
| CEO 产品口径与优先级 | CEO Product Owner | Huyan Web | Sales/Store/Product/Inventory/Customer Owners |
| 指标与数据对账 | Data Core Owner | 各数据域 Owner | SAP Owner、Finance |
| 身份、权限与审计 | Security/Auth Owner | Gateway/Auth | Huyan、Data Core |
| AI 输出与证据 | AI Owner | AI/Agent Owners | Data Owners、Legal/Security |
| Guardian 与 SLO | SRE Owner | Platform/SRE | 各模块 Owner |
| 发布与回滚 | Release Manager | Web + SRE | Auth、Core、AI、业务 Owner |

---

## 14. 最终交付声明

VAFOX V6.1.10 的正确上线形态不是再建一个 CEO 数据系统，而是把已存在且受治理的能力组织成一个一致、可追溯、可降级、可回滚的 CEO 决策入口。

生产完成标准为：

- CEO 只需从 `huyan.vafox.com` 进入，即可完成“今日总览 → 分域分析 → 证据下钻 → AI 咨询 → 人工决策”。
- 销售、店铺、员工、顾客、供应商、商品、库存、客户、AI 日报和 Guardian 全部复用现有能力。
- SAP、Data Core、Auth 的事实、接口、身份与治理职责不变。
- 任一结论都能说明来源、口径、范围、截止时间和局限；任一敏感访问都经过授权并可审计。
- AI 保持顾问身份，执行权继续属于人和既有受控工作流。

满足第 11 节全部 P0 门禁并完成灰度观察后，VAFOX V6.1.10 可签署为 **CEO OS Production Ready**。
