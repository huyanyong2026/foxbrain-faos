# Local-only PostgreSQL credential. Do not reuse a production credential.
POSTGRES_PASSWORD=replace-with-a-long-local-only-password

# Optional host-port overrides.
ORCHESTRATOR_API_PORT=8001
ORCHESTRATOR_WEB_PORT=3001
